; unified context diff
(setq diff-switches "-u")

; colored diff
;; now handled by default in emacs 24.3
;(custom-set-faces
; '(diff-added ((t (:foreground "DarkGreen"))) 'now)
; '(diff-removed ((t (:foreground "DarkRed"))) 'now)
; )

(provide 'my-diff)
